import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:listacompra/app/modules/list/item/item_page.dart';

void main() {
  testWidgets('ItemPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ItemPage(title: 'Item')));
    //  final titleFinder = find.text('Item');
    //  expect(titleFinder, findsOneWidget);
  });
}
